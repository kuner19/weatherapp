import { StyleSheet } from "react-native";
import { StatusBar } from "react-native";
import {Fonts} from "../../../constants/Sizes"


const loc_style = StyleSheet.create({
    locations : (width) => ({
        padding:10,
        borderBottomColor: 'black',
        borderWidth : .2
        
    }),
 })


 

 export default loc_style